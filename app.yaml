# Google Cloud App Engine 部署設定
# gcloud app deploy
runtime: custom
env: flex

automatic_scaling:
  min_num_instances: 0
  max_num_instances: 2

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
